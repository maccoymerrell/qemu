# isaxcheck fixup assertion table — the repairs the plugin makes to the
# decode boundary, asserted rather than tolerated.
#
# Author: Maccoy Merrell
# SPDX-License-Identifier: GPL-2.0-or-later
#
# WHAT THIS FILE IS
# -----------------
# `isaxcheck --fixups` runs the tracer's decode boundary and the tracer's own
# decode_detail_to_generic() over the same encodings and reports every place
# the second changes the first.  That difference is not noise and it is not a
# residual: it IS the set of defects the plugin repairs downstream of
# disas/capstone.c, plus the classification the ISA tables supply that
# Capstone has no opinion about.
#
# It is asserted BOTH WAYS.  A line here that stops matching fails the gate
# (dead rule) — that is a repair that has silently stopped happening.  A
# repair that appears and is not written down here also fails.  The boundary
# gate cannot do either: measured against the boundary alone, a regressed
# repair reintroduces exactly the disagreement the boundary allowlist already
# expects, so it cancels out and nothing turns red.  RISC-V `ret` is the
# worked example — the boundary loses its `ra` read, the plugin restores it,
# and roughly a billion dynamic instructions per reference trace depend on
# that restoration.
#
# Format:  <isa> <signature key>          (`#` starts a whole-line comment)
# Keys are `W-<what>-<direction> <mnemonic> <delta>`, with register bank
# indices collapsed to `#`.  `-added` / `-dropped` are from the plugin's
# point of view: `W-rd-added ret +REG_LR` means the dependency model reads a
# register the boundary did not report.
#
# HOW TO CHANGE IT.  Do not edit a line to make the gate green.  A line that
# has gone dead is either a repair that regressed (fix the plugin) or one that
# became unnecessary because Capstone was fixed (delete the line, and check
# whether the matching entry in isaxcheck_allow.txt can go too).  A new line
# is either a repair somebody added without recording it, or the dependency
# model changing what it records — both want a sentence here saying which.
#
# Observed against Capstone 6.0.0-Alpha7 (subprojects/capstone.wrap).
# Regenerate the raw set with:
#   isaxcheck --isa=<isa> --jobs=N --fixups


# --- aarch64 -----------------------------------------------------------
#
# Trap and exception-generating instructions.  Capstone reports no control
# group for BRK / HLT / UDF / DCPS<n>, and reports HVC / SMC / SVC as plain
# calls; the ISA table classifies all of them as BRANCH_SYSCALL_TYPE, which
# is what the tracer's BB chain needs to seal a block at a trap.  DRPS is
# the reverse: Capstone calls it a control transfer and the table does not,
# because a debug-state exception return is not an edge any consumer can
# follow.
#
# SVC is the load-bearing member and it is the one that was missing.  Every
# system call in an AArch64 trace is an SVC, and this reclassification is
# what makes the BB seal there.  The row could not be written before,
# because the encoding sat outside the swept space: exception generation
# carries its whole selector in [4:0] — 0b00000 BRK/HLT, 0b00001 SVC,
# 0b00010 HVC, 0b00011 SMC — and no fill offered Rd = 1.  The siblings that
# WERE recorded are exactly the ones whose selector the old fills happened
# to carry.  Rd = 1 and Rd = 3 complete the column (see sweep_aarch64), and
# with them SVC, SMC, DCPS1 and DCPS3.
aarch64 W-flow-added brk BRANCH_SYSCALL_TYPE
aarch64 W-flow-added dcps1 BRANCH_SYSCALL_TYPE
aarch64 W-flow-added dcps2 BRANCH_SYSCALL_TYPE
aarch64 W-flow-added dcps3 BRANCH_SYSCALL_TYPE
aarch64 W-flow-added hlt BRANCH_SYSCALL_TYPE
aarch64 W-flow-added udf BRANCH_SYSCALL_TYPE
aarch64 W-flow-dropped drps BRANCH_NONE
aarch64 W-flow-reclassed hvc BRANCH_SYSCALL_TYPE
aarch64 W-flow-reclassed smc BRANCH_SYSCALL_TYPE
aarch64 W-flow-reclassed svc BRANCH_SYSCALL_TYPE

# SVE2.1 / SME2 predicate-PAIR destinations.  `whilege { p14.b, p15.b }, ...`
# writes two predicate registers and Capstone reports only the first; the
# table supplies the second.  Note what this means for the OTHER gate: the
# `aarch64 R-wr-missing while* +p#` entry in isaxcheck_allow.txt describes a
# disagreement that never reaches a trace, because the repair asserted here
# removes it one layer down.  That is the false-positive half of why this
# file exists.
aarch64 W-wr-added pext +REG_PRED#
aarch64 W-wr-added whilegt +REG_PRED#
aarch64 W-wr-added whilehi +REG_PRED#
aarch64 W-wr-added whilele +REG_PRED#
aarch64 W-wr-added whilels +REG_PRED#

# --- riscv64 -----------------------------------------------------------
#
# Branch taxonomy.  Capstone has no control group for the RISC-V aliases at
# all, so every classification below comes from the ISA table: J / JAL / JALR
# / RET, and the ECALL / EBREAK / UNIMP traps.
riscv64 W-flow-added ebreak BRANCH_SYSCALL_TYPE
riscv64 W-flow-added ecall BRANCH_SYSCALL_TYPE
riscv64 W-flow-added j BRANCH_DIRECT_JUMP
riscv64 W-flow-added jal BRANCH_DIRECT_CALL
riscv64 W-flow-added unimp BRANCH_SYSCALL_TYPE
riscv64 W-flow-reclassed jalr BRANCH_INDIRECT_CALL
riscv64 W-flow-reclassed ret BRANCH_RETURN

# THE LINK-REGISTER REPAIR.  The aliased forms hide `ra` COMPLETELY in
# Capstone 6 -- it is in neither the operand list nor the (always empty for
# RISC-V) implicit lists -- so without apply_isa_branch_fixups() a call's
# return-address write and a return's read vanish from the dataflow
# entirely.  This is the single highest-weight repair in the tracer, and it
# is the reason this file exists: the boundary gate reports the missing
# write as a defect (isaxcheck_allow.txt, `riscv64 R-wr-missing jal +r#`),
# so a regression here would restore a disagreement the boundary gate is
# already expecting and nothing would turn red.
#
# Only the ALIASED forms are on this list.  `dret` / `mret` / `sret` are
# not: a privileged return resumes from mepc/sepc/dpc rather than from
# `ra`, so the fixup keys off the alias that hides the link register and
# not off the resulting BRANCH_RETURN, which those instructions also carry.
riscv64 W-rd-added ret +REG_LR
riscv64 W-wr-added jal +REG_LR
riscv64 W-wr-added jalr +REG_LR

# The C-extension HINT code points, finished.  C.ADDI with nzimm == 0 and
# C.SLLI / C.SRLI / C.SRAI with shamt == 0 are reserved as HINTs, and the
# unprivileged spec says a HINT does not modify architectural state.
# Capstone agrees on the write half — it reports no destination for any of
# them — and leaves the READ on, which describes an instruction that waits
# for a producer and then delivers nothing.  The register field on a HINT
# is payload, naming which hint this is, not a value it consumes, so the
# dependency model drops the read and the code point arrives as the no-op
# it is.  The boundary is left alone deliberately: an operand with no
# access bits is how it says Capstone supplied no DIRECTION, which sends
# the whole instruction down the positional fallback and would turn the
# payload register into a destination.
#
# One line per register class rather than one per mnemonic, because the
# class is what the key carries and `c.addi ra, 0` and `c.addi sp, 0` land
# in different ones.  Correct-path weight is zero — no compiler emits these
# — and they are reachable because the wrong path decodes arbitrary bytes.
riscv64 W-rd-dropped c.addi -REG_FP_REG
riscv64 W-rd-dropped c.addi -REG_GPR#
riscv64 W-rd-dropped c.addi -REG_LR
riscv64 W-rd-dropped c.addi -REG_SP
riscv64 W-rd-dropped c.slli64 -REG_FP_REG
riscv64 W-rd-dropped c.slli64 -REG_GPR#
riscv64 W-rd-dropped c.slli64 -REG_LR
riscv64 W-rd-dropped c.slli64 -REG_SP
riscv64 W-rd-dropped c.srai64 -REG_FP_REG
riscv64 W-rd-dropped c.srai64 -REG_GPR#
riscv64 W-rd-dropped c.srli64 -REG_FP_REG
riscv64 W-rd-dropped c.srli64 -REG_GPR#

# RVV whole-register load/store.  `vl8re64.v v8, (a0)` moves EIGHT vector
# registers and Capstone names only the base; the tracer expands the group,
# which is what makes the dependency edges for the other seven exist.
riscv64 W-rd-added vs2r.v +REG_VEC#
riscv64 W-rd-added vs4r.v +REG_VEC#
riscv64 W-rd-added vs8r.v +REG_VEC#
riscv64 W-wr-added vl2r.v +REG_VEC#
riscv64 W-wr-added vl2re16.v +REG_VEC#
riscv64 W-wr-added vl2re32.v +REG_VEC#
riscv64 W-wr-added vl2re64.v +REG_VEC#
riscv64 W-wr-added vl4r.v +REG_VEC#
riscv64 W-wr-added vl4re16.v +REG_VEC#
riscv64 W-wr-added vl4re32.v +REG_VEC#
riscv64 W-wr-added vl4re64.v +REG_VEC#
riscv64 W-wr-added vl8r.v +REG_VEC#
riscv64 W-wr-added vl8re16.v +REG_VEC#
riscv64 W-wr-added vl8re32.v +REG_VEC#
riscv64 W-wr-added vl8re64.v +REG_VEC#
# `vmv2r.v` is the same group expansion in move form; it needs rd = v0 to
# print its two-operand shape, which is why it appears only once (1, 0)
# joined the RISC-V sweep fills.
riscv64 W-rd-added vmv2r.v +REG_VEC#
riscv64 W-wr-added vmv2r.v +REG_VEC#

# --- mipsel ------------------------------------------------------------
#
# Traps and exception returns: Capstone reports no control group for
# SYSCALL / BREAK / SDBBP / ERET / DERET.
#
# `jr` reclassed to BRANCH_RETURN is the `jr $ra` recognition -- the ISA has
# no dedicated return instruction, so the return is a register jump whose
# register happens to be $ra, and only the operand text says so.
#
# The branch-and-link forms (BAL / BGEZAL / BGEZALL / BLTZAL / BLTZALL) do
# not appear: they carry BRANCH_DIRECT_CALL plus MF_CONDITIONAL from the
# classification table itself, so the boundary and the dependency model
# agree and there is no repair to record.  They are calls that may not
# happen -- they write $ra, and a consumer driving a return-address stack
# needs the push.
mipsel W-flow-added break BRANCH_SYSCALL_TYPE
mipsel W-flow-added deret BRANCH_RETURN
mipsel W-flow-added eret BRANCH_RETURN
mipsel W-flow-added sdbbp BRANCH_SYSCALL_TYPE
mipsel W-flow-added syscall BRANCH_SYSCALL_TYPE
mipsel W-flow-reclassed jr BRANCH_RETURN

# Traps, interrupt/system returns and the LOOP family: control transfers
# Capstone gives no group for.  The `bnd`-prefixed and `repz`-prefixed
# call/return forms are reclassifications rather than additions -- the
# prefix is a hint, and the instruction under it is still the call or the
# return it always was.
x86_64 W-flow-added int BRANCH_SYSCALL_TYPE
x86_64 W-flow-added int1 BRANCH_SYSCALL_TYPE
x86_64 W-flow-added int3 BRANCH_SYSCALL_TYPE
x86_64 W-flow-added iretl BRANCH_RETURN
x86_64 W-flow-added iretq BRANCH_RETURN
x86_64 W-flow-added iretw BRANCH_RETURN
x86_64 W-flow-added loop BRANCH_COND_DIRECT
x86_64 W-flow-added loope BRANCH_COND_DIRECT
x86_64 W-flow-added loopne BRANCH_COND_DIRECT
x86_64 W-flow-added syscall BRANCH_SYSCALL_TYPE
x86_64 W-flow-added sysenter BRANCH_SYSCALL_TYPE
x86_64 W-flow-added sysexitl BRANCH_SYSCALL_TYPE
x86_64 W-flow-added sysexitq BRANCH_RETURN
x86_64 W-flow-added sysretl BRANCH_RETURN
x86_64 W-flow-added sysretq BRANCH_RETURN
x86_64 W-flow-added ud0 BRANCH_SYSCALL_TYPE
x86_64 W-flow-added ud1 BRANCH_SYSCALL_TYPE
x86_64 W-flow-added ud2 BRANCH_SYSCALL_TYPE
x86_64 W-flow-added vmcall BRANCH_SYSCALL_TYPE
x86_64 W-flow-added vmmcall BRANCH_SYSCALL_TYPE
x86_64 W-flow-reclassed bnd callq BRANCH_REP
x86_64 W-flow-reclassed bnd lretl BRANCH_REP
x86_64 W-flow-reclassed bnd lretq BRANCH_REP
x86_64 W-flow-reclassed bnd retq BRANCH_REP

# --- x86_64 ------------------------------------------------------------
#
# REP / REPE / REPNE promote a string operation to a self-looping branch.
# Capstone reports the prefix and the string op; the tracer turns the pair
# into BRANCH_REP, which is what lets the body emitter fan one TB execution
# into per-iteration entries on a one-instruction self-loop sub-template.
# Without the promotion a `rep movsq` would be one entry however many bytes
# it moved.
x86_64 W-flow-added rep insb BRANCH_REP
x86_64 W-flow-added rep insl BRANCH_REP
x86_64 W-flow-added rep insw BRANCH_REP
x86_64 W-flow-added rep lodsb BRANCH_REP
x86_64 W-flow-added rep lodsl BRANCH_REP
x86_64 W-flow-added rep lodsq BRANCH_REP
x86_64 W-flow-added rep lodsw BRANCH_REP
x86_64 W-flow-added rep movsb BRANCH_REP
x86_64 W-flow-added rep movsl BRANCH_REP
x86_64 W-flow-added rep movsq BRANCH_REP
x86_64 W-flow-added rep movsw BRANCH_REP
x86_64 W-flow-added rep outsb BRANCH_REP
x86_64 W-flow-added rep outsl BRANCH_REP
x86_64 W-flow-added rep outsw BRANCH_REP
x86_64 W-flow-added rep stosb BRANCH_REP
x86_64 W-flow-added rep stosl BRANCH_REP
x86_64 W-flow-added rep stosq BRANCH_REP
x86_64 W-flow-added rep stosw BRANCH_REP
x86_64 W-flow-added repe cmpsb BRANCH_REP
x86_64 W-flow-added repe cmpsl BRANCH_REP
x86_64 W-flow-added repe cmpsq BRANCH_REP
x86_64 W-flow-added repe cmpsw BRANCH_REP
x86_64 W-flow-added repe scasb BRANCH_REP
x86_64 W-flow-added repe scasl BRANCH_REP
x86_64 W-flow-added repe scasq BRANCH_REP
x86_64 W-flow-added repe scasw BRANCH_REP
x86_64 W-flow-added repne cmpsb BRANCH_REP
x86_64 W-flow-added repne cmpsl BRANCH_REP
x86_64 W-flow-added repne cmpsq BRANCH_REP
x86_64 W-flow-added repne insb BRANCH_REP
x86_64 W-flow-added repne insl BRANCH_REP
x86_64 W-flow-added repne lodsb BRANCH_REP
x86_64 W-flow-added repne lodsl BRANCH_REP
x86_64 W-flow-added repne lodsq BRANCH_REP
x86_64 W-flow-added repne movsb BRANCH_REP
x86_64 W-flow-added repne movsl BRANCH_REP
x86_64 W-flow-added repne movsq BRANCH_REP
x86_64 W-flow-added repne outsb BRANCH_REP
x86_64 W-flow-added repne outsl BRANCH_REP
x86_64 W-flow-added repne scasb BRANCH_REP
x86_64 W-flow-added repne scasl BRANCH_REP
x86_64 W-flow-added repne scasq BRANCH_REP
x86_64 W-flow-added repne stosb BRANCH_REP
x86_64 W-flow-added repne stosl BRANCH_REP
x86_64 W-flow-added repne stosq BRANCH_REP
x86_64 W-flow-added xacquire xchgb BRANCH_REP
x86_64 W-flow-added xacquire xchgl BRANCH_REP
x86_64 W-flow-added xrelease xchgb BRANCH_REP
x86_64 W-flow-added xrelease xchgl BRANCH_REP
x86_64 W-flow-reclassed repz retq BRANCH_REP
x86_64 W-flow-reclassed repz retw BRANCH_REP

# Multi-byte NOP: `nopw 0(%rax,%rax,1)` carries a full addressing operand
# that computes nothing.  The tracer records the address inputs, because the
# encoding really does read those registers even though the result is
# discarded -- a consumer modelling the AGU sees the same work the hardware
# does.
#
# `bswapw` and `ffreep` are encodings whose behaviour the architecture does
# not define (a 16-bit BSWAP) or that no assembler emits (FFREEP is the
# undocumented FFREE+pop).  The table gives them a destination; Capstone
# gives them none.
x86_64 W-rd-added nopl +REG_GPR#
x86_64 W-rd-added nopq +REG_GPR#
x86_64 W-rd-added nopw +REG_GPR#
x86_64 W-wr-added bswapw +REG_FP_REG
x86_64 W-wr-added bswapw +REG_GPR#
x86_64 W-wr-added bswapw +REG_SP
x86_64 W-wr-added ffreep +REG_FPR#
x86_64 W-wr-added fstpnce +REG_FPR#

# CET shadow-stack ops.  Capstone reports their register operand with no
# access bits at all (the `Z-access0` defect class), so the boundary loses
# it; the ISA table supplies the edge.  Reached only once ModRM.reg joined
# the x86 sweep -- these live in the 0x0F 0x1E and 0x0F 0xAE groups, where
# reg selects the operation.
x86_64 W-rd-added rdsspq +REG_GPR#
x86_64 W-rd-added umonitor +REG_GPR#
x86_64 W-wr-added incsspd +REG_GPR#
x86_64 W-wr-added incsspq +REG_GPR#
x86_64 W-wr-added rdsspd +REG_GPR#

# The rm half of the ModRM sweep reaches the rest of these same repairs.
#
# The multi-byte NOP and the CET shadow-stack ops above were listed for
# `+REG_GPR#` only because the sweep pinned ModRM.rm to %rcx; rm = 4 and
# rm = 5 are %rsp and %rbp, which the tracer maps to their own generic ids
# rather than folding them into the GPR bank, so the same repair keys a
# separate signature for each.  Nothing new is being repaired here -- these
# are the stack and frame pointers arriving in a field that was pinned.
x86_64 W-rd-added nopl +REG_FP_REG
x86_64 W-rd-added nopl +REG_SP
x86_64 W-rd-added nopq +REG_FP_REG
x86_64 W-rd-added nopq +REG_SP
x86_64 W-rd-added nopw +REG_FP_REG
x86_64 W-rd-added nopw +REG_SP
x86_64 W-rd-added rdsspq +REG_FP_REG
x86_64 W-rd-added rdsspq +REG_SP
x86_64 W-rd-added umonitor +REG_FP_REG
x86_64 W-rd-added umonitor +REG_SP
x86_64 W-wr-added incsspd +REG_FP_REG
x86_64 W-wr-added incsspd +REG_SP
x86_64 W-wr-added incsspq +REG_FP_REG
x86_64 W-wr-added incsspq +REG_SP
x86_64 W-wr-added rdsspd +REG_FP_REG
x86_64 W-wr-added rdsspd +REG_SP

# The BND prefix reclassification, on the three spellings the wider sweep
# adds.  `bnd retq` and `bnd callq` were already listed; BND reuses the F2
# byte that is otherwise REPNE, and the tracer's branch classifier keys on
# the prefix, so the whole `bnd` family reads as BRANCH_REP.  That is the
# existing behaviour being asserted on more encodings, not a new one -- and
# it is written down here precisely so that if the classifier is ever taught
# to tell BND from REPNE, these lines go dead and say so.
x86_64 W-flow-reclassed bnd lretw BRANCH_REP
x86_64 W-flow-reclassed bnd notrack callq BRANCH_REP
x86_64 W-flow-reclassed bnd retw BRANCH_REP

# XBEGIN is the one genuinely new KIND of repair the rm sweep exposes, and
# it is a deliberate modelling decision rather than a defect on either side.
# `c7 f8` sits at ModRM.reg = 7, rm = 0, so no pinned rm reached it.
# Capstone puts XBEGIN in a jump group because its abort path is a control
# transfer; the tracer classifies it as fall-through, which is what keeps the
# true-BB chain correct -- the abort edge has no target the trace could name,
# and QEMU's TCG x86 translator implements no RTM at all, so XBEGIN cannot
# retire on a correct path in the first place.
x86_64 W-flow-dropped xbegin BRANCH_NONE

# The repairs the WIDENED x86 sweep (REX as a full field, ModRM.mod = 01/10,
# and the whole VEX space) newly asserts.  Two kinds, and both are the
# existing machinery being held in place on more encodings rather than new
# behaviour.
#
# 1. The 16-bit REPNE string forms.  `66 f2 a7` and its siblings become
# reachable once mod = 10 lets a string opcode sit where the ModRM byte
# goes.  The tracer's branch classifier keys on the REP/REPNE prefix and
# gives them BRANCH_REP, which the boundary does not carry -- the same
# reclassification already listed for the 32- and 64-bit spellings.
x86_64 W-flow-added repne cmpsw BRANCH_REP
x86_64 W-flow-added repne insw BRANCH_REP
x86_64 W-flow-added repne lodsw BRANCH_REP
x86_64 W-flow-added repne movsw BRANCH_REP
x86_64 W-flow-added repne outsw BRANCH_REP
x86_64 W-flow-added repne scasw BRANCH_REP
x86_64 W-flow-added repne stosw BRANCH_REP

# 2. The operand walker recovering registers Capstone dropped.  Capstone
# 6.0.0-Alpha7 reports access == 0 on the register operands of the AVX-512
# mask ops (kadd*, ktest*, kunpck*) and of vpermil2pd/ps, vbroadcasti128 and
# vcvtpd2psx, so the boundary's implicit read/write sets come back EMPTY --
# `--hex=c5c54ac0` (`kaddb %k0,%k7,%k0`) shows boundary RD{} WR{} against
# fields RD{REG_PRED0,REG_PRED7} WR{REG_PRED0}.  The plugin's own operand
# walker reads the operand list rather than the access flags and puts the
# dependencies back.  These lines are what proves that recovery still
# happens: the boundary-layer allowlist records the same encodings as a
# Capstone defect (Z-access0), and WITHOUT these entries a regression in the
# walker would cancel out against that record and the gate would stay green
# while the trace lost the registers.  The MEM half of the same Capstone
# defect is NOT recovered -- see the cause-4 block in isaxcheck_allow.txt.
x86_64 W-rd-added kaddb +REG_PRED#
x86_64 W-rd-added kaddd +REG_PRED#
x86_64 W-rd-added kaddq +REG_PRED#
x86_64 W-rd-added kaddw +REG_PRED#
x86_64 W-rd-added ktestb +REG_PRED#
x86_64 W-rd-added ktestd +REG_PRED#
x86_64 W-rd-added ktestq +REG_PRED#
x86_64 W-rd-added ktestw +REG_PRED#
x86_64 W-rd-added kunpckdq +REG_PRED#
x86_64 W-rd-added kunpckwd +REG_PRED#
x86_64 W-rd-added vpermil2pd +REG_VEC#
x86_64 W-rd-added vpermil2ps +REG_VEC#
x86_64 W-wr-added kaddb +REG_PRED#
x86_64 W-wr-added kaddd +REG_PRED#
x86_64 W-wr-added kaddq +REG_PRED#
x86_64 W-wr-added kaddw +REG_PRED#
x86_64 W-wr-added ktestb +REG_PRED#
x86_64 W-wr-added ktestd +REG_PRED#
x86_64 W-wr-added ktestq +REG_PRED#
x86_64 W-wr-added ktestw +REG_PRED#
x86_64 W-wr-added kunpckdq +REG_PRED#
x86_64 W-wr-added kunpckwd +REG_PRED#
x86_64 W-wr-added vbroadcasti128 +REG_VEC#
x86_64 W-wr-added vcvtpd2psx +REG_VEC#
x86_64 W-wr-added vpermil2pd +REG_VEC#
x86_64 W-wr-added vpermil2ps +REG_VEC#

# JALR.HB is JALR with a hazard barrier: it links, so it is a call.  The
# boundary carries no call group for it; the tracer's branch classifier
# gives it BRANCH_INDIRECT_CALL, which is what keeps it out of the plain
# indirect-jump bucket in the branch taxonomy.  Reachable only once `sa`
# is swept (JALR.HB is JALR with bit 10 set, i.e. sa = 16).
mipsel W-flow-added jalr.hb BRANCH_INDIRECT_CALL
