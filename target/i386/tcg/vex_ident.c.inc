/*
 * Auto-generated by scripts/x86_vex_ident_instrument.py -- do not edit.
 *
 * QEMU's own identity for the two ENCODINGS of a decode-table
 * row that serves both.  The i386 table is indexed by opcode,
 * and a VEX prefix only supplies the implied 0F/66/F2/F3
 * bytes, so one __LINE__ slot answers for the legacy SSE
 * spelling of an instruction and for its VEX spelling alike.
 * Where the two classify differently the slot cannot say
 * which it is, and the row publishes nothing at all.
 *
 * The discriminator is not missing: decode_insn() sets
 * PREFIX_VEX before the row is consulted.  These rows carry
 * it into the identity.
 *
 * The name is `decode-new/<mnemonic>@vex=<bit>`: the rule's
 * own name in QEMU's source, qualified by the encoding bit,
 * in the same `@`-suffixed form decodetree writes when one
 * trans_ function is reached from several patterns.
 *
 * THE NAME IS NOT UNIQUE AND THE ID IS -- the contract's own
 * split.  QEMU names a table row for the generator it
 * dispatches to, so two rows name MOVQ and three name
 * VMOVLPx, and 472 of the 854 base slots already share a name
 * with another slot.  The id is therefore FNV-1a 32 -- the
 * derivation scripts/decodetree.py uses -- of the name with
 * the base slot appended, printed beside each row so it can be
 * recomputed by hand.  A qualified id is exactly as stable as
 * the base id it is folded from, and no more.
 *
 * THE BASE ROWS DO NOT MOVE.  decode-new.c.inc is not edited
 * apart from the one inserted call, which sits BELOW every
 * X86_OP_* site so no slot is renumbered, and a slot that is
 * not qualified here keeps the row's own id and name bit for
 * bit.  Both arms of a qualified slot are present: the
 * selector is checked over the whole (slot, vex) space, so a
 * qualified row cannot fall back to the base identity for one
 * value of the bit and publish for the other.
 */

static const struct {
    uint32_t id;
    const char *name;
} vex_ident_tab[] = {
    { 0, NULL },
    /* MOVD_from legacy encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/MOVD_from@vex=0#551" */
    /* decode-new.c.inc:551 */
    { 0x7ff65894u, "decode-new/MOVD_from@vex=0" },
    /* MOVD_from VEX encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/MOVD_from@vex=1#551" */
    /* decode-new.c.inc:551 */
    { 0xb580027bu, "decode-new/MOVD_from@vex=1" },
    /* MOVD_from legacy encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/MOVD_from@vex=0#552" */
    /* decode-new.c.inc:552 */
    { 0x82f65d4du, "decode-new/MOVD_from@vex=0" },
    /* MOVD_from VEX encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/MOVD_from@vex=1#552" */
    /* decode-new.c.inc:552 */
    { 0xb680040eu, "decode-new/MOVD_from@vex=1" },
    /* MOVQ legacy encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/MOVQ@vex=0#553" */
    /* decode-new.c.inc:553 */
    { 0x4e673a9au, "decode-new/MOVQ@vex=0" },
    /* MOVQ VEX encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/MOVQ@vex=1#553" */
    /* decode-new.c.inc:553 */
    { 0xa7f33281u, "decode-new/MOVQ@vex=1" },
    /* MOVQ legacy encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/MOVQ@vex=0#618" */
    /* decode-new.c.inc:618 */
    { 0xd2c0ce9eu, "decode-new/MOVQ@vex=0" },
    /* MOVQ VEX encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/MOVQ@vex=1#618" */
    /* decode-new.c.inc:618 */
    { 0xe3fe6645u, "decode-new/MOVQ@vex=1" },
    /* VMOVLPx legacy encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VMOVLPx@vex=0#973" */
    /* decode-new.c.inc:973 */
    { 0xecac981bu, "decode-new/VMOVLPx@vex=0" },
    /* VMOVLPx VEX encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VMOVLPx@vex=1#973" */
    /* decode-new.c.inc:973 */
    { 0x85fea1e0u, "decode-new/VMOVLPx@vex=1" },
    /* VMOVSD_ld legacy encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VMOVSD_ld@vex=0#980" */
    /* decode-new.c.inc:980 */
    { 0xdf973185u, "decode-new/VMOVSD_ld@vex=0" },
    /* VMOVSD_ld VEX encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VMOVSD_ld@vex=1#980" */
    /* decode-new.c.inc:980 */
    { 0x183f6d6au, "decode-new/VMOVSD_ld@vex=1" },
    /* VMOVLPx legacy encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VMOVLPx@vex=0#996" */
    /* decode-new.c.inc:996 */
    { 0xdf89f162u, "decode-new/VMOVLPx@vex=0" },
    /* VMOVLPx VEX encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VMOVLPx@vex=1#996" */
    /* decode-new.c.inc:996 */
    { 0x9b215531u, "decode-new/VMOVLPx@vex=1" },
    /* VMOVLPx_st legacy encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VMOVLPx_st@vex=0#1003" */
    /* decode-new.c.inc:1003 */
    { 0x78101a1eu, "decode-new/VMOVLPx_st@vex=0" },
    /* VMOVLPx_st VEX encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VMOVLPx_st@vex=1#1003" */
    /* decode-new.c.inc:1003 */
    { 0xe81e2f97u, "decode-new/VMOVLPx_st@vex=1" },
    /* VMOVLPx legacy encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VMOVLPx@vex=0#1027" */
    /* decode-new.c.inc:1027 */
    { 0xa733a08cu, "decode-new/VMOVLPx@vex=0" },
    /* VMOVLPx VEX encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VMOVLPx@vex=1#1027" */
    /* decode-new.c.inc:1027 */
    { 0x1f41c29du, "decode-new/VMOVLPx@vex=1" },
    /* VCVTSI2Sx legacy encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VCVTSI2Sx@vex=0#1074" */
    /* decode-new.c.inc:1074 */
    { 0x4b67d58eu, "decode-new/VCVTSI2Sx@vex=0" },
    /* VCVTSI2Sx VEX encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VCVTSI2Sx@vex=1#1074" */
    /* decode-new.c.inc:1074 */
    { 0xe401508fu, "decode-new/VCVTSI2Sx@vex=1" },
    /* VCVTSI2Sx legacy encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VCVTSI2Sx@vex=0#1075" */
    /* decode-new.c.inc:1075 */
    { 0x4c67d721u, "decode-new/VCVTSI2Sx@vex=0" },
    /* VCVTSI2Sx VEX encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VCVTSI2Sx@vex=1#1075" */
    /* decode-new.c.inc:1075 */
    { 0xe3014efcu, "decode-new/VCVTSI2Sx@vex=1" },
    /* VMOVLPx_st legacy encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VMOVLPx_st@vex=0#1087" */
    /* decode-new.c.inc:1087 */
    { 0x6c238ef2u, "decode-new/VMOVLPx_st@vex=0" },
    /* VMOVLPx_st VEX encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VMOVLPx_st@vex=1#1087" */
    /* decode-new.c.inc:1087 */
    { 0xd40a8863u, "decode-new/VMOVLPx_st@vex=1" },
    /* VMOVLPx_st legacy encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VMOVLPx_st@vex=0#1208" */
    /* decode-new.c.inc:1208 */
    { 0x4c3ea5e9u, "decode-new/VMOVLPx_st@vex=0" },
    /* VMOVLPx_st VEX encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/VMOVLPx_st@vex=1#1208" */
    /* decode-new.c.inc:1208 */
    { 0x02b2561cu, "decode-new/VMOVLPx_st@vex=1" },
    /* VCMP legacy encoding, X86_OP_ENTRY4; FNV-1a-32 of "decode-new/VCMP@vex=0#1303" */
    /* decode-new.c.inc:1303 */
    { 0x8a0a186bu, "decode-new/VCMP@vex=0" },
    /* VCMP VEX encoding, X86_OP_ENTRY4; FNV-1a-32 of "decode-new/VCMP@vex=1#1303" */
    /* decode-new.c.inc:1303 */
    { 0x0374fb5au, "decode-new/VCMP@vex=1" },
    /* MOVD_to legacy encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/MOVD_to@vex=0#1394" */
    /* decode-new.c.inc:1394 */
    { 0xa83c17e9u, "decode-new/MOVD_to@vex=0" },
    /* MOVD_to VEX encoding, X86_OP_ENTRY3; FNV-1a-32 of "decode-new/MOVD_to@vex=1#1394" */
    /* decode-new.c.inc:1394 */
    { 0x77af7d50u, "decode-new/MOVD_to@vex=1" },
};

/*
 * The qualified rows a slot reaches, indexed by the VEX
 * bit.  Sorted by slot; a slot absent here is not
 * qualified and keeps its own identity.
 */
static const struct {
    uint32_t slot;
    uint16_t idx[2];
} vex_ident_slots[] = {
    {  551, {  1,  2 } },
    {  552, {  3,  4 } },
    {  553, {  5,  6 } },
    {  618, {  7,  8 } },
    {  973, {  9, 10 } },
    {  980, { 11, 12 } },
    {  996, { 13, 14 } },
    { 1003, { 15, 16 } },
    { 1027, { 17, 18 } },
    { 1074, { 19, 20 } },
    { 1075, { 21, 22 } },
    { 1087, { 23, 24 } },
    { 1208, { 25, 26 } },
    { 1303, { 27, 28 } },
    { 1394, { 29, 30 } },
};

static inline uint16_t vex_ident_of(uint32_t slot, bool vex)
{
    for (size_t i = 0; i < ARRAY_SIZE(vex_ident_slots); i++) {
        if (vex_ident_slots[i].slot == slot) {
            return vex_ident_slots[i].idx[vex];
        }
    }
    return 0;
}

/*
 * Publish the encoding-qualified identity for a slot that
 * has one.  A slot that has none publishes nothing and
 * keeps what the unconditional publish above recorded.
 */
static inline void vex_ident_publish(uint32_t slot, bool vex)
{
    uint16_t idx = vex_ident_of(slot, vex);

    if (idx) {
        plugin_gen_record_insn_identity(vex_ident_tab[idx].id,
                                        vex_ident_tab[idx].name);
    }
}
