#ifndef CHAMPSIM_TRACER_QEMU_REGS_RISCV_H
#define CHAMPSIM_TRACER_QEMU_REGS_RISCV_H

/*
 * QEMU-indexed register table for riscv -- auto-generated by
 * champsim_tracer_mnemonic_audit.py --qemu-regs.  Do not hand-edit a row.
 *
 * One row per register QEMU can NAME: its GDB-stub
 * namespace -- what qemu_plugin_get_registers() reports --
 * plus the register files the target DECLARES to the
 * dataflow extractor (insn_dataflow_declare_regfile), which
 * insn_dataflow_field_reg() answers in and the stub need not
 * carry.  Sorted by (feature, name) so a consumer can
 * bisect.  This is the
 * authority for what generic dependency slot a register is;
 * the Capstone-keyed table is a route to these rows and is
 * cross-checked against them at install.
 *
 * .tier says whether the Capstone key can reach the row:
 *   QREG_ROUTED   at least one Capstone register id maps here
 *   QREG_UNNAMED  QEMU carries the register and Capstone's enum
 *                 has no id for it, so no Capstone-fed operand
 *                 can ever name it.  These rows are reachable
 *                 only by QEMU identity.
 *   QREG_PREDICATED  QEMU carries it only when a run-time
 *                 predicate admits it (the RISC-V CSR space,
 *                 filtered through csr_ops[].predicate).  The
 *                 row answers what generic word the NAME is;
 *                 it does not stand for its class's VALUE.
 *   QREG_DECLARED  QEMU names it only through a target
 *                 declaration and its GDB stub does not carry
 *                 it, so no handle can read the register.
 *                 Same division as QREG_PREDICATED: the row
 *                 answers the NAME, never the VALUE.
 *
 * SPDX-License-Identifier: GPL-2.0-or-later
 * Author: Maccoy Merrell
 */

/* riscv: 468 QEMU registers, 104 reachable from
 * Capstone, 364 reachable only by QEMU identity,
 * 0 of those named by a target declaration rather than by
 * the GDB stub, 467 carrying a generic id. */
static const QemuRegRow qemu_regs_riscv[] = {
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "a0", .reg_id = REG_GPR10, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "a1", .reg_id = REG_GPR11, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "a2", .reg_id = REG_GPR12, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "a3", .reg_id = REG_GPR13, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "a4", .reg_id = REG_GPR14, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "a5", .reg_id = REG_GPR15, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "a6", .reg_id = REG_GPR16, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "a7", .reg_id = REG_GPR17, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "fp", .reg_id = REG_FP_REG, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "gp", .reg_id = REG_GPR3, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "pc", .reg_id = REG_NONE, .cap_rows = 0, .tier = QREG_UNNAMED },
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "ra", .reg_id = REG_LR, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "s1", .reg_id = REG_GPR9, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "s10", .reg_id = REG_GPR26, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "s11", .reg_id = REG_GPR27, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "s2", .reg_id = REG_GPR18, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "s3", .reg_id = REG_GPR19, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "s4", .reg_id = REG_GPR20, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "s5", .reg_id = REG_GPR21, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "s6", .reg_id = REG_GPR22, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "s7", .reg_id = REG_GPR23, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "s8", .reg_id = REG_GPR24, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "s9", .reg_id = REG_GPR25, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "sp", .reg_id = REG_SP, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "t0", .reg_id = REG_GPR5, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "t1", .reg_id = REG_GPR6, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "t2", .reg_id = REG_GPR7, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "t3", .reg_id = REG_GPR28, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "t4", .reg_id = REG_GPR29, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "t5", .reg_id = REG_GPR30, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "t6", .reg_id = REG_GPR31, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "tp", .reg_id = REG_GPR4, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.cpu", .name = "zero", .reg_id = REG_ZERO, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "cycle", .reg_id = REG_SYSTIMER, .cap_rows = 0, .tier = QREG_PREDICATED },  /* a counter that advances on its own -- MIPS Count's group.  A read of it depends on nothing in the instruction stream */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "cycleh", .reg_id = REG_SYSTIMER, .cap_rows = 0, .tier = QREG_PREDICATED },  /* a counter that advances on its own -- MIPS Count's group.  A read of it depends on nothing in the instruction stream */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "fcsr", .reg_id = REG_FCSR, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the FP (fcsr) and vector (vcsr) control-and-status files addressed WHOLE; frm/fflags and vxrm/vxsat are their fields and already carry REG_FCSR, so the container meets the word its members have */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "fflags", .reg_id = REG_FCSR, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "frm", .reg_id = REG_FCSR, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hcounteren", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* a PERMISSION mask -- which counters a lower privilege level may read -- not counter state.  MIPS HWREna is the same register in the same role and sits on REG_SYS */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hedeleg", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* which traps are delegated: read on the same event that writes the rest of the trap file */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hedelegh", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* which traps are delegated: read on the same event that writes the rest of the trap file */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "henvcfg", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "henvcfgh", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hgatp", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hgeie", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hgeip", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hideleg", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* which traps are delegated: read on the same event that writes the rest of the trap file */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hidelegh", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* which traps are delegated: read on the same event that writes the rest of the trap file */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hie", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hip", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter10", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter10h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter11", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter11h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter12", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter12h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter13", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter13h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter14", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter14h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter15", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter15h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter16", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter16h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter17", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter17h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter18", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter18h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter19", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter19h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter20", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter20h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter21", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter21h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter22", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter22h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter23", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter23h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter24", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter24h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter25", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter25h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter26", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter26h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter27", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter27h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter28", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter28h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter29", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter29h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter3", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter30", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter30h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter31", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter31h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter3h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter4", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter4h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter5", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter5h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter6", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter6h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter7", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter7h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter8", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter8h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter9", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hpmcounter9h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hstateen0", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hstateen0h", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hstateen1", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hstateen1h", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hstateen2", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hstateen2h", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hstateen3", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hstateen3h", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hstatus", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the status word a trap writes and an *ret restores */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "htimedelta", .reg_id = REG_SYSTIMER, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the compare value that fires against the free-running counter, and the guest offset applied to it */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "htimedeltah", .reg_id = REG_SYSTIMER, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the compare value that fires against the free-running counter, and the guest offset applied to it */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "htinst", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "htval", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hvictl", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hvien", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hvienh", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hvip", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hviph", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hviprio1", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hviprio1h", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hviprio2", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "hviprio2h", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "instret", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "instreth", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "jvt", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the Zcmt jump-vector-table base: a control register the table jump reads, with no MMU, trap or counter role */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "marchid", .reg_id = REG_SYSID, .cap_rows = 0, .tier = QREG_PREDICATED },  /* read-only implementation identification and configuration; a read of one can depend on nothing, so it must not share an ID with writable state */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mcause", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mconfigptr", .reg_id = REG_SYSID, .cap_rows = 0, .tier = QREG_PREDICATED },  /* read-only implementation identification and configuration; a read of one can depend on nothing, so it must not share an ID with writable state */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mcontext", .reg_id = REG_SYSDBG, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the trigger module and the control-transfer-record buffer: debug, watchpoint and trace state */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mcounteren", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* a PERMISSION mask -- which counters a lower privilege level may read -- not counter state.  MIPS HWREna is the same register in the same role and sits on REG_SYS */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mcountinhibit", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the performance counters' own control: inhibit, overflow status and the Smcntrpmf privilege filter.  REG_SYSPERF is 'counters AND their control' */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mctrctl", .reg_id = REG_SYSDBG, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the trigger module and the control-transfer-record buffer: debug, watchpoint and trace state */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mcycle", .reg_id = REG_SYSTIMER, .cap_rows = 0, .tier = QREG_PREDICATED },  /* a counter that advances on its own -- MIPS Count's group.  A read of it depends on nothing in the instruction stream */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mcyclecfg", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the performance counters' own control: inhibit, overflow status and the Smcntrpmf privilege filter.  REG_SYSPERF is 'counters AND their control' */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mcyclecfgh", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the performance counters' own control: inhibit, overflow status and the Smcntrpmf privilege filter.  REG_SYSPERF is 'counters AND their control' */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mcycleh", .reg_id = REG_SYSTIMER, .cap_rows = 0, .tier = QREG_PREDICATED },  /* a counter that advances on its own -- MIPS Count's group.  A read of it depends on nothing in the instruction stream */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "medeleg", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* which traps are delegated: read on the same event that writes the rest of the trap file */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "medelegh", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* which traps are delegated: read on the same event that writes the rest of the trap file */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "menvcfg", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "menvcfgh", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mepc", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhartid", .reg_id = REG_SYSID, .cap_rows = 0, .tier = QREG_PREDICATED },  /* read-only implementation identification and configuration; a read of one can depend on nothing, so it must not share an ID with writable state */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter10", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter10h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter11", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter11h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter12", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter12h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter13", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter13h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter14", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter14h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter15", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter15h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter16", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter16h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter17", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter17h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter18", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter18h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter19", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter19h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter20", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter20h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter21", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter21h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter22", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter22h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter23", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter23h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter24", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter24h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter25", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter25h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter26", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter26h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter27", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter27h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter28", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter28h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter29", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter29h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter3", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter30", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter30h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter31", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter31h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter3h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter4", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter4h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter5", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter5h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter6", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter6h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter7", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter7h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter8", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter8h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter9", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmcounter9h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent10", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent10h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent11", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent11h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent12", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent12h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent13", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent13h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent14", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent14h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent15", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent15h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent16", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent16h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent17", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent17h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent18", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent18h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent19", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent19h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent20", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent20h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent21", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent21h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent22", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent22h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent23", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent23h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent24", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent24h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent25", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent25h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent26", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent26h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent27", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent27h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent28", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent28h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent29", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent29h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent3", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent30", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent30h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent31", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent31h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent3h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent4", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent4h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent5", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent5h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent6", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent6h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent7", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent7h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent8", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent8h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent9", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mhpmevent9h", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mideleg", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* which traps are delegated: read on the same event that writes the rest of the trap file */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "midelegh", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* which traps are delegated: read on the same event that writes the rest of the trap file */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mie", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mieh", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mimpid", .reg_id = REG_SYSID, .cap_rows = 0, .tier = QREG_PREDICATED },  /* read-only implementation identification and configuration; a read of one can depend on nothing, so it must not share an ID with writable state */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "minstret", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "minstretcfg", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the performance counters' own control: inhibit, overflow status and the Smcntrpmf privilege filter.  REG_SYSPERF is 'counters AND their control' */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "minstretcfgh", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the performance counters' own control: inhibit, overflow status and the Smcntrpmf privilege filter.  REG_SYSPERF is 'counters AND their control' */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "minstreth", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* counters that advance on hardware events and the event selectors that say which -- MIPS PerfCnt/PerfCtl's group */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mip", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "miph", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mireg", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mireg2", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mireg3", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mireg4", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mireg5", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mireg6", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "misa", .reg_id = REG_SYSID, .cap_rows = 0, .tier = QREG_PREDICATED },  /* read-only implementation identification and configuration; a read of one can depend on nothing, so it must not share an ID with writable state */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "miselect", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mncause", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mnepc", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mnscratch", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* scratch -- REG_SYS names scratch explicitly.  The architecture gives these no role beyond holding whatever the handler put there */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mnstatus", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the status word a trap writes and an *ret restores */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mscratch", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* scratch -- REG_SYS names scratch explicitly.  The architecture gives these no role beyond holding whatever the handler put there */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mseccfg", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mstateen0", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mstateen0h", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mstateen1", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mstateen1h", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mstateen2", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mstateen2h", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mstateen3", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mstateen3h", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mstatus", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the status word a trap writes and an *ret restores */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mstatush", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the status word a trap writes and an *ret restores */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mtinst", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mtopei", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mtopi", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mtval", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mtval2", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mtvec", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mvendorid", .reg_id = REG_SYSID, .cap_rows = 0, .tier = QREG_PREDICATED },  /* read-only implementation identification and configuration; a read of one can depend on nothing, so it must not share an ID with writable state */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mvien", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mvienh", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mvip", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "mviph", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpaddr0", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpaddr1", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpaddr10", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpaddr11", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpaddr12", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpaddr13", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpaddr14", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpaddr15", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpaddr2", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpaddr3", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpaddr4", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpaddr5", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpaddr6", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpaddr7", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpaddr8", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpaddr9", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpcfg0", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpcfg1", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpcfg2", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "pmpcfg3", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "satp", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "scause", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "scounteren", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* a PERMISSION mask -- which counters a lower privilege level may read -- not counter state.  MIPS HWREna is the same register in the same role and sits on REG_SYS */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "scountinhibit", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the performance counters' own control: inhibit, overflow status and the Smcntrpmf privilege filter.  REG_SYSPERF is 'counters AND their control' */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "scountovf", .reg_id = REG_SYSPERF, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the performance counters' own control: inhibit, overflow status and the Smcntrpmf privilege filter.  REG_SYSPERF is 'counters AND their control' */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sctrctl", .reg_id = REG_SYSDBG, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the trigger module and the control-transfer-record buffer: debug, watchpoint and trace state */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sctrdepth", .reg_id = REG_SYSDBG, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the trigger module and the control-transfer-record buffer: debug, watchpoint and trace state */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sctrstatus", .reg_id = REG_SYSDBG, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the trigger module and the control-transfer-record buffer: debug, watchpoint and trace state */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "seed", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the Zkr entropy source: a read with a side effect whose value depends on nothing architectural.  Not REG_SYSID -- it is not a constant */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "senvcfg", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sepc", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sie", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sieh", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sip", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "siph", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sireg", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sireg2", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sireg3", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sireg4", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sireg5", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sireg6", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "siselect", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sscratch", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* scratch -- REG_SYS names scratch explicitly.  The architecture gives these no role beyond holding whatever the handler put there */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "ssp", .reg_id = REG_SSP, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sstateen0", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sstateen1", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sstateen2", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sstateen3", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* permission and enable masks (Zicboz/Svpbmt/pointer-masking gating, Smstateen).  Not the FP/vector execution gate REG_SYSFPEN names -- these admit unrelated extensions one bit each */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "sstatus", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the status word a trap writes and an *ret restores */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "stimecmp", .reg_id = REG_SYSTIMER, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the compare value that fires against the free-running counter, and the guest offset applied to it */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "stimecmph", .reg_id = REG_SYSTIMER, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the compare value that fires against the free-running counter, and the guest offset applied to it */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "stopei", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "stopi", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "stval", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "stvec", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "tdata1", .reg_id = REG_SYSDBG, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the trigger module and the control-transfer-record buffer: debug, watchpoint and trace state */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "tdata2", .reg_id = REG_SYSDBG, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the trigger module and the control-transfer-record buffer: debug, watchpoint and trace state */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "tdata3", .reg_id = REG_SYSDBG, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the trigger module and the control-transfer-record buffer: debug, watchpoint and trace state */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "time", .reg_id = REG_SYSTIMER, .cap_rows = 0, .tier = QREG_PREDICATED },  /* a counter that advances on its own -- MIPS Count's group.  A read of it depends on nothing in the instruction stream */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "timeh", .reg_id = REG_SYSTIMER, .cap_rows = 0, .tier = QREG_PREDICATED },  /* a counter that advances on its own -- MIPS Count's group.  A read of it depends on nothing in the instruction stream */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "tinfo", .reg_id = REG_SYSDBG, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the trigger module and the control-transfer-record buffer: debug, watchpoint and trace state */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "tselect", .reg_id = REG_SYSDBG, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the trigger module and the control-transfer-record buffer: debug, watchpoint and trace state */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vcsr", .reg_id = REG_FCSR, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the FP (fcsr) and vector (vcsr) control-and-status files addressed WHOLE; frm/fflags and vxrm/vxsat are their fields and already carry REG_FCSR, so the container meets the word its members have */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vl", .reg_id = REG_VCTRL, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vlenb", .reg_id = REG_SYSID, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vsatp", .reg_id = REG_SYSMMU, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the address-check machinery consulted on every access: the translation roots and the physical-memory-protection rules.  A write to either invalidates the same checks, which is the edge REG_SYSMMU exists to carry */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vscause", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vsctrctl", .reg_id = REG_SYSDBG, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the trigger module and the control-transfer-record buffer: debug, watchpoint and trace state */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vsepc", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vsie", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vsieh", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vsip", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vsiph", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vsireg", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vsireg2", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vsireg3", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vsireg4", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vsireg5", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vsireg6", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vsiselect", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* an indirect access WINDOW whose content the companion *iselect chooses at run time.  No one behaviour group holds for every selectable target, so the window register itself -- not a guessed target -- is what the guest orders against */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vsscratch", .reg_id = REG_SYS, .cap_rows = 0, .tier = QREG_PREDICATED },  /* scratch -- REG_SYS names scratch explicitly.  The architecture gives these no role beyond holding whatever the handler put there */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vsstatus", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the status word a trap writes and an *ret restores */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vstart", .reg_id = REG_VCTRL, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the vector element start index: read and written by every vector instruction alongside vl and vtype, which are REG_VCTRL */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vstimecmp", .reg_id = REG_SYSTIMER, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the compare value that fires against the free-running counter, and the guest offset applied to it */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vstimecmph", .reg_id = REG_SYSTIMER, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the compare value that fires against the free-running counter, and the guest offset applied to it */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vstopei", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vstopi", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* the interrupt file -- pending, enable, virtual-interrupt injection and the AIA top-interrupt views.  An interrupt IS a trap in this ISA, and these are written and read on the same event as mepc and mcause */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vstval", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vstvec", .reg_id = REG_SYSEXC, .cap_rows = 0, .tier = QREG_PREDICATED },  /* written together BY the trap and read by its handler -- the vector base, the return PC, the cause and the faulting value */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vtype", .reg_id = REG_VCTRL, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vxrm", .reg_id = REG_FCSR, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.csr", .name = "vxsat", .reg_id = REG_FCSR, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fa0", .reg_id = REG_FPR10, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fa1", .reg_id = REG_FPR11, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fa2", .reg_id = REG_FPR12, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fa3", .reg_id = REG_FPR13, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fa4", .reg_id = REG_FPR14, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fa5", .reg_id = REG_FPR15, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fa6", .reg_id = REG_FPR16, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fa7", .reg_id = REG_FPR17, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fs0", .reg_id = REG_FPR8, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fs1", .reg_id = REG_FPR9, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fs10", .reg_id = REG_FPR26, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fs11", .reg_id = REG_FPR27, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fs2", .reg_id = REG_FPR18, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fs3", .reg_id = REG_FPR19, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fs4", .reg_id = REG_FPR20, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fs5", .reg_id = REG_FPR21, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fs6", .reg_id = REG_FPR22, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fs7", .reg_id = REG_FPR23, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fs8", .reg_id = REG_FPR24, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "fs9", .reg_id = REG_FPR25, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "ft0", .reg_id = REG_FPR0, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "ft1", .reg_id = REG_FPR1, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "ft10", .reg_id = REG_FPR30, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "ft11", .reg_id = REG_FPR31, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "ft2", .reg_id = REG_FPR2, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "ft3", .reg_id = REG_FPR3, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "ft4", .reg_id = REG_FPR4, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "ft5", .reg_id = REG_FPR5, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "ft6", .reg_id = REG_FPR6, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "ft7", .reg_id = REG_FPR7, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "ft8", .reg_id = REG_FPR28, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.fpu", .name = "ft9", .reg_id = REG_FPR29, .cap_rows = 3, .tier = QREG_ROUTED },  /* 3 capstone rows */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v0", .reg_id = REG_VEC0, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v1", .reg_id = REG_VEC1, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v10", .reg_id = REG_VEC10, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v11", .reg_id = REG_VEC11, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v12", .reg_id = REG_VEC12, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v13", .reg_id = REG_VEC13, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v14", .reg_id = REG_VEC14, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v15", .reg_id = REG_VEC15, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v16", .reg_id = REG_VEC16, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v17", .reg_id = REG_VEC17, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v18", .reg_id = REG_VEC18, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v19", .reg_id = REG_VEC19, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v2", .reg_id = REG_VEC2, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v20", .reg_id = REG_VEC20, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v21", .reg_id = REG_VEC21, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v22", .reg_id = REG_VEC22, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v23", .reg_id = REG_VEC23, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v24", .reg_id = REG_VEC24, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v25", .reg_id = REG_VEC25, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v26", .reg_id = REG_VEC26, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v27", .reg_id = REG_VEC27, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v28", .reg_id = REG_VEC28, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v29", .reg_id = REG_VEC29, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v3", .reg_id = REG_VEC3, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v30", .reg_id = REG_VEC30, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v31", .reg_id = REG_VEC31, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v4", .reg_id = REG_VEC4, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v5", .reg_id = REG_VEC5, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v6", .reg_id = REG_VEC6, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v7", .reg_id = REG_VEC7, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v8", .reg_id = REG_VEC8, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
    { .feature = "org.gnu.gdb.riscv.vector", .name = "v9", .reg_id = REG_VEC9, .cap_rows = 1, .tier = QREG_ROUTED },  /* 1 capstone row */
};

static const size_t qemu_regs_riscv_count =
    sizeof(qemu_regs_riscv) / sizeof(qemu_regs_riscv[0]);

#endif /* CHAMPSIM_TRACER_QEMU_REGS_RISCV_H */
